#include <stdio.h>
#include <stdlib.h>
#include "main.h"

/**
 * _abs - function to get the absolute value of an integer
 *@a: integer to transform
 * Return: the absolute value
 */

int _abs(int n)
{
	n = abs(n);
	return (n);
}
