#include "main.h

#include <stdlib.h>

char *create_array(unsigned int size, char c)
{
	if (size == 0)
	{return NULL;

	}
	char *arc;
	int i;

	arc = malloc(size * sizeof(char));
	arc[0] = c;
	i = 0;
	while (i < size)
	{
		_putchar (arc[i]);
	}
