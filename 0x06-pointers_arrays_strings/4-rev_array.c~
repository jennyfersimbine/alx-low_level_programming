#include "main.h"
#include <string.h>

void reverse_array(int *a, int n)
{
	int a[] = *a;
	int aid[];

	for (int i = 0; i < n; i++)
	{
		aid[n - 1 - i] = arr[i];
	}
	for (int i = 0; i < n; i++)
	{
		arr[i] = aux[i];
	}
}
