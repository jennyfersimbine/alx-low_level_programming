#include "main.h"
#include <string.h>

/**
 * _strcmp - compare strings
 * @s1: string one
 * @s2: string two
 * Return: 0
 */
int _strcmp(char *s1, char *s2)
{
	int ans;

	ans = strcmp(s1, s2);
	return (ans);
}
