#include <stdio.h>
#include <string.h>

/**
 * _strlen - return length of a string
 * @s: pointer paramenter
 * Return: length of string
 */

int _strlen(char *s)
{
	int length;

	length_str = strlen(*s);
	return (length_str);
}
