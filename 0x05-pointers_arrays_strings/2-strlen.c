#include <stdio.h>

/**
 * _strlen - return length of a string
 * @s: pointer paramenter
 * Return: length of string
 */

int _strlen(char *s)
{
	int length;
	
	length = sizeof(*s);
	return (length);
}
