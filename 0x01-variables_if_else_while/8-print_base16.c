#include <stdio.h>
#include <stdlib.h>

/**
* main - Entry point
*
* Return: Always return 0 (Success)
*/

int main(void)
{
	int i;

	for (i = 96; i < 122; i++)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
