#include <stdio.h>
#include <stdlib.h>

/**
 * main - Entry point
 *
 *Return: Always 0 (Success)
 */

int main(void)
{
	char alphab;

	alphab = 'a';
	while (alphab <= 'z')
	{
		putchar(alphab);
		alphab = alphab + 1;
	}
	putchar(
	return (0);
}
